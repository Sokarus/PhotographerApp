import {TextColorMap as TextColorMapType} from '@type/style';

const TextColorMap: TextColorMapType = {
  black: 'black',
  white: 'rgb(191, 191, 191)',
};

const ColorTheme: {[key: string]: 'black' | 'white'} = {
  black: 'black',
  white: 'white',
};

export {TextColorMap, ColorTheme};
